

const Title2 = ({children}) => {
    return (
        <h1 className='md:text-xl text-lg text-primary dark:text-white font-bold'>
            {children}
        </h1>
    );
};

export default Title2;